import { Clapperboard } from "lucide-react"
import { SectionPlaceholder } from "@/components/section-placeholder"

export default function ShortsPage() {
  return (
    <SectionPlaceholder
      icon={Clapperboard}
      title="Shorts"
      description="A vertical, full-screen short-video feed with swipe navigation and creator controls is coming next."
    />
  )
}
